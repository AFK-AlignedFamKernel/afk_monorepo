{
  "name": "indexer-prisma",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm prisma generate && pnpm prisma db push && tsc",
    "build:push": "pnpm prisma generate && pnpm prisma db push && tsc",
    "build:pull": "pnpm prisma generate && pnpm prisma db pull && tsc",
    "generate": "pnpm prisma generate",
    "pull": "pnpm prisma db pull",
    "push": "pnpm prisma db push",
    "migrate": "pnpm prisma migrate dev",
    "migrate:prod": "pnpm prisma migrate deploy",
    "diff": "pnpm prisma migrate diff"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "starknet": "^6.6.6",
    "tsup": "^8.0.2",
    "typescript": "~5.3.3"
  }
}
