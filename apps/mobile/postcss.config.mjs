/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // 'nativewind/postcss': {
    //   tailwindConfig: './tailwind.config.js',
    // },
  },
};

export default config;
