// import { StreamList } from '@/components/StreamList';

import StreamListWrapper from '@/components/StreamList/ClientWrapper';

export default function StreamsPage() {
  return (
    <main className="min-h-screen bg-gray-50">
      <StreamListWrapper />
    </main>
  );
} 